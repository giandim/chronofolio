{
  "name": "chrono-folio",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx http-server dist -c-1",
    "dev": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --loader:.css=file --watch",
    "start:dev": "concurrently \"npm run dev\" \"npm run start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "esbuild": "^0.25.1",
    "typescript": "^5.8.2"
  }
}
